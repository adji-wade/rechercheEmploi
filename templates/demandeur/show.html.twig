{% extends 'base.html.twig' %}

{% block title %}Demandeur{% endblock %}

{% block body %}


    <h1>Demandeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demandeur.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ demandeur.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ demandeur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('demandeur_index') }}">back to list</a>

    <a href="{{ path('demandeur_edit', {'id': demandeur.id}) }}">edit</a>

    {{ include('demandeur/_delete_form.html.twig') }}
{% endblock %}
